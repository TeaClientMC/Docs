---
const { src } = Astro.props;
---

<div class="image" style={`--image-url: url(${src})`}></div>

<style>
.image {
    transform: perspective(800px) rotateY(-15deg) translateY(10px) rotateX(10deg) scale(1);
    transition: transform 0.6s ease;
    box-shadow: 0 0 40px rgb(0, 0, 0);
    position: relative;
    background: var(--image-url) center center / cover;
    flex-basis: 100%;
    width: 100%;
    border-radius: 1rem;
}

.image:hover {
    transform: perspective(800px) rotateY(-15deg) translateY(10px) rotateX(10deg) scale(1.1);
}
</style>